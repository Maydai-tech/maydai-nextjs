/**
 * Alias URL pour le webhook Stripe (même handler que `app/api/stripe/webhook`).
 * Configure l’endpoint Stripe sur l’URL qui vous convient.
 */
export { POST } from '@/app/api/stripe/webhook/route'
